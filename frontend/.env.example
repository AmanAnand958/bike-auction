# Frontend environment variables (optional - only needed for production deploys)
# In dev, the Vite proxy handles routing to the backend

# Only set this if you're deploying the frontend separately (e.g. Vercel)
# VITE_API_URL=https://your-backend.onrender.com/api
